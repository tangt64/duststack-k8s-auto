- name: install tc package for kubernetes
  package:
    name: "{{ item }}"
    state: present
  loop:
    - iproute
    - iproute-tc  

- name: install the kubeadm package for install
  package:
    name: kubeadm
    state: present